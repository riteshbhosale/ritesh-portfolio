const ROOTS = {
  ROOT: "/",
};

export const paths = {
  home: {
    root: ROOTS.ROOT,
    projects: {
      root : `${ROOTS.ROOT}projects`,
      chatApp: `${ROOTS.ROOT}projects/chat-app`,
    },
  },
};
